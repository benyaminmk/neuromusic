

    # from synthesizer import Player, Synthesizer, Waveform     # Use Python 2.7.16
    # from time import sleep

    # p = Player()
    # p.open_stream()

    # s = Synthesizer(osc1_waveform=Waveform.sine, osc1_volume=1.0, use_osc2=False)

    # # chord = ["C3", "E3", "G3"]
    # # p.play_wave(s.generate_chord(chord, 3.0))

    # # chord = [440.0, 550.0, 660.0]
    # # p.play_wave(s.generate_chord(chord, 3.0))

    # #   d = delta   < 4Hz
    # #   t = theta   4 - 8Hz
    # #   a = alpha   8 - 12Hz
    # #   b = betta   12 - 30Hz
    # #   g = gamma   > 30Hz

    # # scale data
    # # quantize data to scale

    # while True:
    #     chord = [d, t, a, b, g]
        
        
    #     p.play_wave(s.generate_chord(chord, 1.0))

    #     # sleep() argument is in seconds
    #     sleep(0.5)

